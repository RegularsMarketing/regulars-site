---
layout: base.njk
pageKey: contact
permalink: /book-a-call/
---
{% set c = site.pages.contact %}
<section style="padding-top:60px;">
  <div class="wrap">
    <div class="contact-hero reveal">
      <div class="eyebrow center">{{ c.eyebrow }}</div>
      <h2>{{ c.heading }}</h2>
      <p>{{ c.subhead }}</p>
    </div>

    <div class="contact-panel reveal">
      <div>
        <h3>{{ c.form_heading }}</h3>
        <p>{{ c.form_subhead }}</p>
        <!-- Formspree: set contact.formspree_contact_id in content/site.yaml -->
        <form class="form-fields" action="https://formspree.io/f/{{ site.contact.formspree_contact_id }}" method="POST">
          {% for f in c.form_fields %}
          <input type="{{ f.type }}" name="{{ f.name }}" placeholder="{{ f.placeholder }}"{% if f.required %} required{% endif %}>
          {% endfor %}
          <input type="hidden" name="_subject" value="{{ c.form_subject }}">
          <input type="text" name="_gotcha" style="display:none" tabindex="-1" autocomplete="off" aria-hidden="true">
          <button type="submit" class="btn btn-primary btn-block" style="margin-top:6px;">{{ c.form_button }}</button>
        </form>
      </div>
      <div>
        <h3>{{ c.direct_heading }}</h3>
        <p>{{ c.direct_subhead }}</p>
        <ul class="contact-info">
          <li><span>{{ site.ui.label_email }}</span><span>{{ site.contact.email }}</span></li>
          <li><span>{{ site.ui.label_instagram }}</span><span>{% if site.contact.instagram_url %}<a href="{{ site.contact.instagram_url }}">{{ site.contact.instagram }}</a>{% else %}{{ site.contact.instagram }}{% endif %}</span></li>
          <li><span>{{ site.ui.label_response }}</span><span>{{ site.contact.response_time }}</span></li>
          <li><span>{{ site.ui.label_works_with }}</span><span>{{ site.contact.works_with }}</span></li>
        </ul>
        {% if site.contact.hours and site.contact.hours.length %}
        <h3 style="margin-top:28px;">{{ site.ui.label_hours }}</h3>
        <ul class="contact-info">
          {% for h in site.contact.hours %}
          <li><span>{{ h.days }}</span><span>{{ h.hours }}</span></li>
          {% endfor %}
        </ul>
        {% endif %}
      </div>
    </div>
  </div>
</section>
