{# PROOF: off — renders nothing.

   The default for the slot, so a site that HAS real social proof (an
   established client past month one) turns the pre-launch statement off from
   Decap rather than by editing a template. Deliberately an empty file: the
   dispatcher resolves it and emits nothing. #}
